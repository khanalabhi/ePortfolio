extends layout

block content

  if err
    div
      h3 Error updating Relative Volume
    hr

  if doc
    div
      h3 Updated Relative Volume
    hr

  p Upate the relative volume of a ticker
  form.subtle(action="/market/update_volume", method="post")
    div.input
      span.label.input-label Username: 
      input.standard-input(type="text", name="username")
    div.input
      span.label.input-label Password: 
      input.standard-input(type="password", name="password")
      br
    div.input
      span.label.input-label Ticker Symbol: 
      input.standard-input(type="text", name="ticker")
    div.input
      span.label.input-label Relative Volume: 
      input.standard-input(type="number", name="volume")
    br
    div.actions
      input.standard-input(type="submit", value="Update")